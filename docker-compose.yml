services:

  wordpress:
    image: wordpress
    restart: always
    ports:
      - '${APP_PORT:-80}:80'
    environment:
      WORDPRESS_DB_HOST: mysql
      WORDPRESS_DB_USER: wp_user
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: wp
      WORDPRESS_DEBUG: true
    networks:
      - zimrate
    volumes:
      - wordpress:/var/www/html
      - '.:/var/www/html/wp-content/plugins/zimrate'
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    ports:
      - '${FORWARD_DB_PORT:-3306}:3306'
    restart: always
    environment:
      MYSQL_DATABASE: wp
      MYSQL_USER: wp_user
      MYSQL_PASSWORD: password
      MYSQL_RANDOM_ROOT_PASSWORD: '1'
    volumes:
      - mysql:/var/lib/mysql
    networks:
      - zimrate
    healthcheck:
      test:
        - CMD
        - mysqladmin
        - ping
        - '-ppassword'
      retries: 3
      timeout: 5s

  phpmyadmin:
    depends_on:
      - mysql
    image: phpmyadmin/phpmyadmin
    environment:
      - PMA_HOST=mysql
      - PMA_PORT=3306
      - PMA_ARBITRARY=0
    ports:
      - '${PMA_PORT:-8080}:80'
    networks:
      - zimrate

networks:
  zimrate:
    driver: bridge

volumes:
  wordpress:
  mysql:
